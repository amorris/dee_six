<%= form_for(@custodian) do |f| %>
  <% if @custodian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custodian.errors.count, "error") %> prohibited this custodian from being saved:</h2>

      <ul>
      <% @custodian.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
